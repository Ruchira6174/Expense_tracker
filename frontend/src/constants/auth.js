/** localStorage key for the JWT access token */
export const TOKEN_STORAGE_KEY = 'token';

/** Auth API route paths (relative to api base URL) */
export const AUTH_ROUTES = {
  register: '/auth/register',
  login: '/auth/login',
  me: '/auth/me',
};
